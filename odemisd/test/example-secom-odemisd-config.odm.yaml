SECOM: {
    class: Microscope,
    role: secom,
    emitters: ["Quanta e-beam", Light],
    detectors: ["Andor Clara", "Quanta SED"],
    actuators: ["Redstone Stage", "Dual-view alignment"],
}

Light: {
    class: simulated.Light,
    role: brightlight,
#    init: ,
    affects: ["Andor Clara"],
    properties: {
        power: 100 # W
    }
}

"Andor Clara": {
    class: andorcam2.AndorCam2,
    role: ccd,
    init: {device: 0} # the only Andor camera connected so always 0
}

"Quanta SED": {
    role: se-detector
}

Quanta: {
    class: quanta.QuantaCOM,
    role: sem,
    init: {host: 192.168.0.10}, # depends on the IP address of the quanta PC
    children: {beam: "Quanta e-beam", sed: "Quanta SED"} # provides also a stage child but not used here
}

"Quanta e-beam": {
    #class not necessary as it's a child
    role: e-beam,
    affects: ["Quanta SED", "Andor Clara"] # affects the CCD in case of cathodoluminescence
}

"Redstone Stage": {
    class: pi.StageRedStone,
    role: stage,
    init: {port: /dev/ttyUSB0, 'x': [1, 1], 'y': [0, 1]}, # addresses of the controllers on the serial bus
    affects: ["Quanta SED", "Andor Clara"],
    properties: {
        speed: 1 #m/s
    }
}

"Dual-view alignment": {
    class: pi.StageRedStone,
    role: lenseshift, # specific role to secom
    init: {port: /dev/tty0, 'x': [0, 1], 'y': [0, 2]}, # having the controllers on the same bus as the stage would require the use of a CombinedActuator
    affects: ["Andor Clara"],
    properties: {
        speed: 0.1 #m/s
    }
}
